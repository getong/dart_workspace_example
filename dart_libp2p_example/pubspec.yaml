name: dart_libp2p_example
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.10.0-290.4.beta

# Add regular dependencies here.
dependencies:
  path: ^1.9.0
  dart_libp2p: ^0.5.3
  dart_udx: ^0.3.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
